# NLP Project — TikTok Semantic Similarity & Hashtag Recommendation
# IE University, 2026
# Install: pip install -r requirements.txt

# Database connectivity
# psycopg2-binary: only needed for download_data.py (not required for the notebook itself)
psycopg2-binary>=2.9
SQLAlchemy>=2.0

# Data manipulation
pandas>=2.0
numpy>=1.24

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Machine learning & NLP
scikit-learn>=1.3
sentence-transformers>=2.2
scipy>=1.11

# Dimensionality reduction
umap-learn>=0.5

# Emoji handling (demojisation for TF-IDF)
emoji>=2.8

# Jupyter
jupyter>=1.0
ipykernel>=6.0

# Optional — fast cosine KNN for large corpora (>10,000 posts)
# The notebook falls back to sklearn brute-force if faiss-cpu is not installed
faiss-cpu>=1.7
